name: Release to Production

on:
  release:
    types:
      - published
  workflow_run:
    workflows: ["Daily Release for Milestone"]
    types: [completed]
  workflow_dispatch:

jobs:
  reset_release_branch:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
